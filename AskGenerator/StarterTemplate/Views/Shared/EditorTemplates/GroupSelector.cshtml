@model string
@{
    var list = (IEnumerable<SelectListItem>)AskGenerator.Site.GroupManager.All().Select(g => new SelectListItem()
                {
                    Text = g.Name,
                    Value = g.Id,
                    Selected = g.Id.Equals(Model)
                }).OrderBy(i => i.Text);
}

@Html.DropDownListFor(m => m, selectList:list)